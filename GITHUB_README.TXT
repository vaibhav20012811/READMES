# git init
# git status
# git add <filename> or . --( . for all)
# git commit -m "test commit"
# git remote add origin <repo link>
# git remote -v
# git branch
# git branch -m <new name> --(to change branch name)

--- push commands ---
# git push origin master
# git push -u origin <new branch name> --(for push multile time then # git push)

# git checkout <branch name> --(to change branch)
# git checkout -b <branch name> --(to create new branch)
# git branch -d <branch name> --(to delete branch)

# git diff <branch name to compair> --(to check differences between 2 branches)
# git merge <branch name> --(to merge two branches)

# git pull origin main --(to download data from branch)

--- undo commands ---
1. added but not commited
# git reset <file name>
# git reset --hard <file name>
# git reset

2. commited
# git reset HEAD~1

